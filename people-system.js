(() => {
  'use strict';
  const PROFILE_KEY='parisPeopleProfileV1', SHARE_KEY='parisPeopleLocationShareV1', DEVICE_KEY='parisPeopleDeviceIdV1';
  const parse=(v,f)=>{try{return v?JSON.parse(v):f}catch{return f}};
  const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const identity=()=>parse(localStorage.getItem('parisIdentityV1'),null);
  const profile=()=>parse(localStorage.getItem(PROFILE_KEY),{avatarData:null,avatarColor:'#e76f91',locationSharing:localStorage.getItem(SHARE_KEY)==='1'});
  const saveProfile=p=>{localStorage.setItem(PROFILE_KEY,JSON.stringify(p));localStorage.setItem(SHARE_KEY,p.locationSharing?'1':'0')};
  const deviceId=(()=>{let x=localStorage.getItem(DEVICE_KEY);if(!x){x=crypto.randomUUID?.()||`dev-${Date.now()}-${Math.random().toString(36).slice(2)}`;localStorage.setItem(DEVICE_KEY,x)}return x})();
  const icons={budget:'💳',gallery:'📷',moment:'♥',people:'👥',planning:'✦',closure:'☾',health:'◌',location:'⌖',profile:'◉',trip:'✦',general:'•'};
  let lastActivity={key:'general',text:'ist in der Paris-App',at:0};
  let heartbeatTimer=null, locationTimer=null, participantCache=[], realtimeChannel=null, currentFeedFilter='all', lastTracked=new Map();

  function device(){const ua=navigator.userAgent||'', p=navigator.platform||'';if(/iPad/i.test(ua)||(/Macintosh/.test(ua)&&navigator.maxTouchPoints>1))return{type:'iPad',icon:'📱',platform:'iPadOS'};if(/iPhone/i.test(ua))return{type:'iPhone',icon:'🍎',platform:'iOS'};if(/Android/i.test(ua))return{type:'Android',icon:'🤖',platform:'Android'};if(/Mac/i.test(p))return{type:'Mac',icon:'💻',platform:'macOS'};if(/Win/i.test(p))return{type:'Windows',icon:'🖥️',platform:'Windows'};return{type:'Web',icon:'🌐',platform:p||'Browser'}}
  const cloud=()=>window.ParisCloud;
  async function rpc(name,args){await cloud()?.ready;const r=await cloud().client.rpc(name,args);if(r.error)throw r.error;return r.data}
  async function ensureProfile(){const id=identity(), p=profile();if(!id?.tripId)return;await rpc('paris_upsert_member_profile',{p_trip_id:id.tripId,p_member_name:id.memberName||'Mitreisend',p_avatar_data:p.avatarData,p_avatar_color:p.avatarColor,p_location_sharing:!!p.locationSharing})}
  async function heartbeat(activity=null){const id=identity();if(!id?.tripId||document.hidden)return;const d=device(), a=activity||lastActivity;try{await rpc('paris_update_presence',{p_trip_id:id.tripId,p_device_id:deviceId,p_member_name:id.memberName||'Mitreisend',p_device_type:d.type,p_platform:d.platform,p_activity_key:a?.key||null,p_activity_text:a?.text||null})}catch(e){console.warn('Präsenz:',e.message)}}
  const FEED_ALLOWED=[
    /^gallery\.(upload|delete|favorite)$/,
    /^moment\.(create|delete|like|comment)$/,
    /^budget\.(add|change|delete|export)$/,
    /^planning\.(change|reserve|visited|complete)$/,
    /^closure\./,
    /^people\.(joined|left|invite_sent)$/,
    /^profile\.avatar$/,
    /^location\.(share_start|share_stop|arrive|leave)$/,
    /^health\.(milestone|sync)$/,
    /^trip\.(start|complete)$/
  ];
  const isFeedEvent=type=>FEED_ALLOWED.some(re=>re.test(String(type||'')));
  const isMeaningfulLive=(key,text,at)=>{
    if(!text||!at||Date.now()-Number(at)>90000)return false;
    if(!['gallery','moment','budget','planning','closure','location'].includes(key))return false;
    return !/synchron|öffnet|schaut|profil|teilnehmer|karte|app/i.test(text);
  };
  async function track(type,data={}){const id=identity();if(!id?.tripId)return;const category=data.category||String(type).split('.')[0]||'general',text=data.text||data.title||type,key=data.aggregateKey||null;const dedupe=data.dedupeMs??1200;const sig=type+'|'+text+'|'+(key||'');const now=Date.now();if(now-(lastTracked.get(sig)||0)<dedupe)return;lastTracked.set(sig,now);if(data.live!==false){lastActivity={key:category,text,at:now};heartbeat(lastActivity)}if(!isFeedEvent(type))return;try{return await rpc('paris_track_event',{p_trip_id:id.tripId,p_member_name:id.memberName||'Mitreisend',p_event_type:type,p_category:category,p_activity_text:text,p_icon:data.icon||icons[category]||'•',p_metadata:data.metadata||{},p_aggregate_key:key,p_aggregate_window_seconds:data.aggregateWindowSeconds||0})}catch(e){try{return await rpc('paris_add_member_activity',{p_trip_id:id.tripId,p_member_name:id.memberName||'Mitreisend',p_activity_key:category,p_activity_text:text})}catch{}console.warn('Event:',e.message)}}
  async function setActivity(key,text,{feed=false,type=null,metadata=null,aggregateKey=null}={}){const id=identity();if(!id?.tripId)return;lastActivity={key,text,at:Date.now()};heartbeat(lastActivity);if(feed)return track(type||`${key}.action`,{category:key,text,metadata,aggregateKey})}
  async function pushLocation(){const id=identity(),p=profile();if(!id?.tripId||!p.locationSharing)return;const s=window.ParisLocation?.getState?.();if(!s?.position)return;try{await rpc('paris_update_member_location',{p_trip_id:id.tripId,p_member_name:id.memberName||'Mitreisend',p_latitude:s.position.latitude,p_longitude:s.position.longitude,p_accuracy:s.position.accuracy||null,p_heading:s.position.heading||null,p_speed:s.position.speed||null,p_place_label:s.place?.label||null})}catch(e){console.warn('Live-Standort:',e.message)}}
  async function setLocationSharing(on){const p=profile();p.locationSharing=!!on;saveProfile(p);await ensureProfile();if(on){try{if(!window.ParisLocation?.isEnabled?.())await window.ParisLocation?.enable?.()}catch{}await pushLocation();setActivity('location','teilt gerade den Live-Standort',{feed:true,type:'location.share_start'})}else setActivity('location','hat die Standortfreigabe beendet',{feed:true,type:'location.share_stop'});return p.locationSharing}
  async function participants(){const id=identity();if(!id?.tripId)return[];participantCache=await rpc('paris_list_participants',{p_trip_id:id.tripId})||[];return participantCache}
  async function feed(){const id=identity();if(!id?.tripId)return[];try{return await rpc('paris_list_activity_events',{p_trip_id:id.tripId,p_limit:120})||[]}catch{return await rpc('paris_list_member_activity',{p_trip_id:id.tripId,p_limit:100})||[]}}
  function ago(v){if(!v)return'Noch nie';const sec=Math.max(0,Math.floor((Date.now()-new Date(v).getTime())/1000));if(sec<15)return'gerade eben';if(sec<60)return`vor ${sec} Sekunden`;const m=Math.floor(sec/60);if(m<60)return`vor ${m} Minute${m===1?'':'n'}`;const h=Math.floor(m/60);if(h<24)return`vor ${h} Stunde${h===1?'':'n'}`;return new Date(v).toLocaleDateString('de-DE')}
  function onlineState(v){if(!v)return{cls:'offline',label:'Offline'};const sec=(Date.now()-new Date(v).getTime())/1000;if(sec<50)return{cls:'online',label:'Online'};if(sec<300)return{cls:'away',label:ago(v)};return{cls:'offline',label:ago(v)}}
  function avatarHtml(m,large=false){const name=m.member_name||'?',initials=name.split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase();return m.avatar_data?`<span class="people-avatar ${large?'large':''}"><img src="${esc(m.avatar_data)}" alt=""></span>`:`<span class="people-avatar ${large?'large':''}" style="--avatar:${esc(m.avatar_color||'#e76f91')}">${esc(initials)}</span>`}
  function distance(a,b){if(!a?.latitude||!b?.latitude)return null;const R=6371e3,p1=a.latitude*Math.PI/180,p2=b.latitude*Math.PI/180,dp=(b.latitude-a.latitude)*Math.PI/180,dl=(b.longitude-a.longitude)*Math.PI/180;const x=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return 2*R*Math.atan2(Math.sqrt(x),Math.sqrt(1-x))}
  function css(){if(document.getElementById('peopleCss'))return;const s=document.createElement('style');s.id='peopleCss';s.textContent=`
  .people-grid{display:grid;gap:12px}.people-card{background:rgba(255,255,255,.82);backdrop-filter:blur(18px);border:1px solid rgba(117,137,151,.14);border-radius:28px;padding:18px;box-shadow:0 18px 50px rgba(52,72,89,.075)}.people-top{display:flex;gap:13px;align-items:center}.people-avatar{--avatar:#e76f91;width:52px;height:52px;border-radius:19px;background:var(--avatar);color:#fff;display:inline-flex;align-items:center;justify-content:center;font-weight:900;font-size:17px;overflow:hidden;flex:0 0 auto;box-shadow:0 10px 25px color-mix(in srgb,var(--avatar) 22%,transparent)}.people-avatar.large{width:82px;height:82px;border-radius:28px;font-size:26px}.people-avatar img{width:100%;height:100%;object-fit:cover}.people-name{font:900 22px Georgia;color:#355066}.people-role{font-size:13px;color:#7b8993;margin-top:3px}.people-status{margin-left:auto;font-size:12px;font-weight:850;padding:8px 11px;border-radius:999px}.people-status.online{background:#eaf8f2;color:#34755a}.people-status.away{background:#fff5e8;color:#9b6722}.people-status.offline{background:#f1f3f4;color:#77838b}.people-summary{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.people-summary span{background:#f3f6f7;border-radius:999px;padding:8px 11px;color:#637681;font-size:12px;font-weight:800}.people-details{margin-top:13px;border-top:1px solid #edf0f1;padding-top:4px}.people-details summary{cursor:pointer;list-style:none;padding:10px 0;color:#607482;font-size:13px;font-weight:850}.people-details summary::-webkit-details-marker{display:none}.people-details summary:after{content:'＋';float:right}.people-details[open] summary:after{content:'−'}.people-info{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;padding-bottom:4px}.people-pill{background:#f5f8f9;border-radius:14px;padding:11px;font-size:12px;color:#5d7180;line-height:1.45}.people-activity{margin-top:10px;padding:11px 13px;background:linear-gradient(135deg,rgba(255,240,245,.9),rgba(239,250,248,.9));border-radius:15px;color:#496171;font-weight:800}.people-now{display:flex;align-items:center;gap:13px;padding:15px 17px;margin-bottom:14px;border-radius:22px;background:linear-gradient(135deg,rgba(255,244,247,.92),rgba(241,250,249,.92));border:1px solid rgba(215,184,195,.35);box-shadow:0 12px 30px rgba(70,90,104,.055)}.people-now small{display:block;font-size:10px;letter-spacing:.17em;color:#a05d74;font-weight:950}.people-now b{display:block;margin-top:4px;color:#496171;font-size:15px;line-height:1.35}.people-now-dot{width:10px;height:10px;border-radius:50%;background:#42b883;box-shadow:0 0 0 5px rgba(66,184,131,.10)}.people-now-dot.idle{background:#b7c0c5;box-shadow:0 0 0 5px rgba(183,192,197,.12)}.people-feed-section{margin-top:30px}.people-feed-title{font:900 clamp(30px,8vw,46px) Georgia;color:#355066;margin:0}.people-feed-subtitle{color:#81909a;line-height:1.5;margin:7px 0 16px}.people-filters{display:flex;gap:8px;overflow-x:auto;padding:2px 1px 13px;scrollbar-width:none;-webkit-overflow-scrolling:touch}.people-filters::-webkit-scrollbar{display:none}.people-filter{border:1px solid rgba(111,132,146,.17);background:rgba(255,255,255,.72);backdrop-filter:blur(12px);border-radius:999px;padding:9px 14px;font-size:13px;font-weight:850;color:#607481;white-space:nowrap;box-shadow:0 5px 16px rgba(57,78,92,.035)}.people-filter.active{background:#405f72;color:#fff;border-color:#405f72;box-shadow:0 8px 22px rgba(64,95,114,.20)}.people-feed{position:relative;padding:2px 0 2px 25px}.people-feed:before{content:'';position:absolute;left:8px;top:12px;bottom:12px;width:1px;background:linear-gradient(#e3c8d1,#dce9e8)}.people-feed-item{position:relative;display:grid;grid-template-columns:42px 1fr auto;gap:12px;align-items:start;padding:12px 0 18px;animation:peopleItemIn .28s ease both}.people-feed-item:before{content:'';position:absolute;left:-21px;top:29px;width:9px;height:9px;border-radius:50%;background:#fff;border:3px solid var(--event-accent,#e76f91);box-shadow:0 0 0 5px #fff}.people-event-icon{width:42px;height:42px;border-radius:15px;background:var(--event-bg,#fff0f4);color:var(--event-accent,#d55f83);display:grid;place-items:center;font-size:18px;font-weight:900}.people-event-name{font-weight:900;color:#385369;font-size:14px}.people-event-text{color:#627783;font-size:14px;line-height:1.4;margin-top:2px}.people-event-time{font-size:11px;color:#99a5ac;white-space:nowrap;padding-top:3px}.people-feed-empty{padding:22px 4px;color:#83919a;text-align:center}.people-map{height:min(510px,57vh);border-radius:22px;overflow:hidden;background:#eaf0f1;position:relative}.people-map-empty{height:100%;display:grid;place-items:center;text-align:center;padding:30px;color:#71818c}.people-map-toolbar{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:12px}.people-map-marker{width:46px;height:46px;border-radius:16px;border:3px solid #fff;box-shadow:0 7px 20px rgba(37,56,70,.3);display:grid;place-items:center;color:#fff;font-weight:950;background:var(--avatar);overflow:hidden}.people-map-marker img{width:100%;height:100%;object-fit:cover}.people-profile-editor{display:grid;grid-template-columns:auto 1fr;gap:18px;align-items:center}.people-colors{display:flex;gap:8px;flex-wrap:wrap}.people-color{width:31px;height:31px;border-radius:50%;border:3px solid #fff;box-shadow:0 0 0 1px #dce1e3;cursor:pointer}.people-color.active{box-shadow:0 0 0 3px #455f71}.leaflet-container{font-family:system-ui,sans-serif}.people-popup-name{font-weight:900;font-size:15px}.people-popup-meta{color:#687985;margin-top:4px}@keyframes peopleItemIn{from{opacity:0;transform:translateY(5px)}to{opacity:1;transform:none}}.people-filters{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:9px;overflow:visible;padding:2px 0 15px}.people-filter{min-width:0;display:flex;align-items:center;justify-content:flex-start;gap:8px;padding:11px 12px;border-radius:17px;text-align:left;line-height:1.1}.people-filter-icon{width:27px;height:27px;border-radius:10px;display:grid;place-items:center;background:#f2f5f6;font-size:14px;flex:0 0 auto}.people-filter-label{overflow:hidden;text-overflow:ellipsis}.people-filter.active .people-filter-icon{background:rgba(255,255,255,.17)}
  .people-invite-backdrop{position:fixed;inset:0;z-index:10050;background:rgba(24,38,48,.38);backdrop-filter:blur(10px);display:flex;align-items:flex-end;justify-content:center;padding:18px;animation:peopleFadeIn .18s ease}.people-invite-sheet{width:min(560px,100%);max-height:min(760px,92vh);overflow:auto;background:rgba(255,255,255,.97);border:1px solid rgba(255,255,255,.8);border-radius:30px;padding:22px;box-shadow:0 30px 80px rgba(25,44,58,.25);animation:peopleSheetIn .24s cubic-bezier(.2,.8,.2,1)}.people-invite-head{display:flex;align-items:flex-start;gap:14px}.people-invite-head h3{font:900 30px Georgia;color:#355066;margin:0}.people-invite-head p{color:#7b8b95;margin:5px 0 0;line-height:1.45}.people-invite-close{margin-left:auto;border:0;background:#f1f4f5;width:40px;height:40px;border-radius:14px;font-size:19px;color:#506776}.people-invite-code{margin:17px 0;padding:14px 16px;border-radius:18px;background:linear-gradient(135deg,#fff2f6,#eef9f7);display:flex;align-items:center;justify-content:space-between;gap:12px}.people-invite-code small{display:block;color:#8b7a83;font-weight:800}.people-invite-code strong{display:block;color:#355066;font-size:18px;letter-spacing:.08em;margin-top:3px}.people-invite-label{display:block;color:#506776;font-size:13px;font-weight:900;margin:16px 0 7px}.people-invite-input{width:100%;box-sizing:border-box;border:1px solid #dce4e7;background:#f8fafb;border-radius:17px;padding:14px 15px;font:inherit;color:#355066;outline:none}.people-invite-input:focus{border-color:#e76f91;box-shadow:0 0 0 4px rgba(231,111,145,.11);background:#fff}.people-invite-actions{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:14px}.people-invite-action{border:0;border-radius:17px;padding:13px 14px;font-weight:900;font-size:14px;cursor:pointer;background:#eef3f5;color:#496171}.people-invite-action.primary{background:#e76f91;color:#fff;box-shadow:0 10px 24px rgba(231,111,145,.24)}.people-invite-action.share{background:#405f72;color:#fff}.people-invite-action.wide{grid-column:1/-1}.people-invite-hint{font-size:12px;color:#8b989f;line-height:1.45;margin:12px 2px 0}.people-invite-error{color:#b65064;font-size:12px;font-weight:800;margin-top:8px;min-height:17px}@keyframes peopleFadeIn{from{opacity:0}to{opacity:1}}@keyframes peopleSheetIn{from{opacity:0;transform:translateY(28px) scale(.98)}to{opacity:1;transform:none}}
  @media(max-width:600px){.people-info{grid-template-columns:1fr}.people-profile-editor{grid-template-columns:1fr;text-align:center}.people-profile-editor .people-avatar{margin:auto}.people-map{height:58vh}.people-feed-item{grid-template-columns:40px 1fr}.people-event-time{grid-column:2;padding-top:0}.people-feed{padding-left:22px}.people-filters{grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.people-filter{padding:10px 11px;font-size:12px}.people-invite-backdrop{padding:0;align-items:flex-end}.people-invite-sheet{border-radius:28px 28px 0 0;padding:20px 18px calc(20px + env(safe-area-inset-bottom));max-height:90vh}.people-invite-actions{grid-template-columns:1fr}.people-invite-action.wide{grid-column:auto}.people-invite-head h3{font-size:27px}}
  `;document.head.appendChild(s)}
  function activityIcon(k){return icons[k]||icons.general}
  function eventCategory(e){return e.category||e.activity_key||'general'}
  function eventIcon(e){return e.icon||activityIcon(eventCategory(e))}
  function eventText(e){const count=Number(e.event_count||1);if(count>1&&e.aggregate_key){if(e.event_type==='gallery.upload')return `hat ${count} neue Fotos hinzugefügt`;return `${e.activity_text||''} (${count}×)`}return e.activity_text||''}
  function visibleEvents(events){return (events||[]).filter(e=>isFeedEvent(e.event_type||`${eventCategory(e)}.legacy`))}
  function eventStyle(category){return ({gallery:['#d55f83','#fff0f4'],moment:['#c65d7c','#fff1f5'],budget:['#557a75','#eef8f5'],planning:['#6d72a7','#f1f1fb'],people:['#55758b','#eef5f8'],location:['#b97948','#fff5ec'],health:['#748b5d','#f2f7ed'],closure:['#6b668e','#f2f0f8'],profile:['#8a6f91','#f6f0f7']}[category]||['#71838e','#f2f5f6'])}
  function renderEventRows(node,events,filter='all'){const clean=visibleEvents(events);const filtered=filter==='all'?clean:clean.filter(e=>eventCategory(e)===filter);node.innerHTML=filtered.length?filtered.map(e=>{const [accent,bg]=eventStyle(eventCategory(e));return`<article class="people-feed-item" style="--event-accent:${accent};--event-bg:${bg}" data-event-type="${esc(e.event_type||'')}"><span class="people-event-icon">${eventIcon(e)}</span><div><div class="people-event-name">${esc(e.member_name)}</div><div class="people-event-text">${esc(eventText(e))}</div></div><time class="people-event-time">${ago(e.updated_at||e.created_at)}</time></article>`}).join(''):'<div class="people-feed-empty">Noch keine besonderen Reisemomente in dieser Kategorie.</div>'}
  function updateNowBanner(node,list){if(!node)return;const active=(list||[]).filter(x=>onlineState(x.last_seen_at).cls!=='offline'&&isMeaningfulLive(x.activity_key,x.activity_text,new Date(x.activity_at||0).getTime())).sort((a,b)=>new Date(b.activity_at||0)-new Date(a.activity_at||0))[0];const html=active?`<div class="people-now-dot"></div><div><small>JETZT GERADE</small><b>${esc(active.member_name)} ${esc(active.activity_text)}</b></div>`:`<div class="people-now-dot idle"></div><div><small>JETZT GERADE</small><b>Alle genießen gerade die Reise. ♥</b></div>`;if(node.dataset.html!==html){node.dataset.html=html;node.innerHTML=html}}

  function participantCards(list,mine){return list.length?list.map(m=>{const st=onlineState(m.last_seen_at),isMe=m.user_id===mine;const live=isMeaningfulLive(m.activity_key,m.activity_text,new Date(m.activity_at||0).getTime());return`<article class="people-card"><div class="people-top">${avatarHtml(m)}<div><div class="people-name">${esc(m.member_name)} ${isMe?'<small style="font:800 11px system-ui;color:#c65d7c">DU</small>':''}</div><div class="people-role">${['owner','admin'].includes(m.member_role)?'Reisebesitzer':'Mitreisend'}</div></div><span class="people-status ${st.cls}">${st.cls==='online'?'● ':''}${st.label}</span></div>${live?`<div class="people-activity">${activityIcon(m.activity_key)} ${esc(m.activity_text)}</div>`:''}<div class="people-summary"><span>📷 ${m.photos||0}</span><span>✦ ${m.moments||0} Momente</span><span>👣 ${(m.steps_today||0).toLocaleString('de-DE')}</span></div><details class="people-details"><summary>Details anzeigen</summary><div class="people-info"><div class="people-pill"><b>${esc(m.device_type||'Gerät')}</b><br>${esc(m.platform||'')}</div><div class="people-pill"><b>Zuletzt aktiv</b><br>${ago(m.last_seen_at)}</div><div class="people-pill"><b>${m.location_sharing?esc(m.place_label||'Standort geteilt'):'Standort privat'}</b><br>${m.location_updated_at?ago(m.location_updated_at):'–'}</div><div class="people-pill"><b>Letzter Datenabgleich</b><br>${ago(m.last_sync_at)}</div></div>${m.latitude?'<button class="pc-btn" data-show-map>Auf Karte zeigen</button>':''}</details></article>`}).join(''):`<div class="pc-card">Noch keine Teilnehmer gefunden.</div>`}
  async function refreshPeopleView(c){
    if(!c?.querySelector('[data-people-list]'))return;
    try{
      const [list,events]=await Promise.all([participants(),feed()]);
      if(!c.querySelector('[data-people-list]'))return;
      const mine=cloud()?.userId;
      updateNowBanner(c.querySelector('[data-people-now]'),list);
      const listNode=c.querySelector('[data-people-list]');
      if(listNode)listNode.innerHTML=participantCards(list,mine);
      c.querySelectorAll('[data-show-map]').forEach(b=>b.onclick=()=>window.ParisProfileCenter?.open?.('map'));
      const feedNode=c.querySelector('[data-people-feed]');
      if(feedNode)renderEventRows(feedNode,events,currentFeedFilter);
    }catch(e){console.warn('Teilnehmer-Realtime:',e.message)}
  }
  function buildInviteUrl(code){const u=new URL(location.href);u.search='';u.hash='';u.searchParams.set('paris_join',code);return u.toString()}
  function closeInviteDialog(){document.querySelector('.people-invite-backdrop')?.remove();document.documentElement.style.overflow=''}
  function openInviteDialog(){
    const id=identity();
    if(!id?.joinCode){alert('Für diese Reise ist noch kein Einladungscode verfügbar.');return}
    closeInviteDialog();
    const url=buildInviteUrl(id.joinCode);
    const trip=id.tripName||'unsere Paris-Reise';
    const subject=`Einladung zu ${trip}`;
    const message=`Hallo!\n\nich möchte dich zu ${trip} in unserer Reise-App einladen.\n\nÖffne diesen Link auf deinem Handy:\n${url}\n\nEinladungscode: ${id.joinCode}`;
    const root=document.createElement('div');
    root.className='people-invite-backdrop';
    root.innerHTML=`<section class="people-invite-sheet" role="dialog" aria-modal="true" aria-label="Teilnehmer einladen"><div class="people-invite-head"><div><h3>Gemeinsam reisen</h3><p>Einladung per E-Mail versenden oder direkt über das Teilen-Menü deines iPhones weitergeben.</p></div><button class="people-invite-close" type="button" aria-label="Schließen">×</button></div><div class="people-invite-code"><div><small>Einladungscode</small><strong>${esc(id.joinCode)}</strong></div><button class="people-invite-action" type="button" data-copy-code>Code kopieren</button></div><label class="people-invite-label" for="peopleInviteEmail">E-Mail-Adresse</label><input class="people-invite-input" id="peopleInviteEmail" type="email" inputmode="email" autocomplete="email" placeholder="name@beispiel.de"><div class="people-invite-error" data-invite-error></div><div class="people-invite-actions"><button class="people-invite-action primary" type="button" data-send-mail>E-Mail vorbereiten</button><button class="people-invite-action share" type="button" data-native-share>Mit Apple teilen</button><button class="people-invite-action wide" type="button" data-copy-link>Einladungslink kopieren</button></div><p class="people-invite-hint">Die E-Mail wird in deiner Mail-App geöffnet und erst nach deiner Bestätigung versendet. „Mit Apple teilen“ öffnet das systemeigene Teilen-Menü für Nachrichten, AirDrop, Mail und weitere Apps.</p></section>`;
    document.body.appendChild(root);document.documentElement.style.overflow='hidden';
    const email=root.querySelector('#peopleInviteEmail'),error=root.querySelector('[data-invite-error]');
    const copied=(btn,text)=>{const old=btn.textContent;btn.textContent=text;setTimeout(()=>btn.textContent=old,1800)};
    root.querySelector('.people-invite-close').onclick=closeInviteDialog;
    root.onclick=e=>{if(e.target===root)closeInviteDialog()};
    root.querySelector('[data-copy-code]').onclick=async e=>{try{await navigator.clipboard.writeText(id.joinCode);copied(e.currentTarget,'Kopiert ✓')}catch{error.textContent='Der Code konnte nicht kopiert werden.'}};
    root.querySelector('[data-copy-link]').onclick=async e=>{try{await navigator.clipboard.writeText(url);copied(e.currentTarget,'Link kopiert ✓');track('people.invite_sent',{category:'people',text:'hat eine Einladung geteilt',live:false})}catch{error.textContent='Der Link konnte nicht kopiert werden.'}};
    root.querySelector('[data-send-mail]').onclick=()=>{const address=email.value.trim();if(!address||!email.checkValidity()){error.textContent='Bitte eine gültige E-Mail-Adresse eingeben.';email.focus();return}error.textContent='';track('people.invite_sent',{category:'people',text:'hat eine Einladung per E-Mail vorbereitet',live:false});location.href=`mailto:${encodeURIComponent(address)}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(message)}`};
    const share=root.querySelector('[data-native-share]');
    if(!navigator.share){share.textContent='Teilen-Menü nicht verfügbar';share.disabled=true}
    share.onclick=async()=>{try{await navigator.share({title:subject,text:`Komm zu ${trip} dazu. Einladungscode: ${id.joinCode}`,url});track('people.invite_sent',{category:'people',text:'hat eine Einladung geteilt',live:false})}catch(e){if(e?.name!=='AbortError')error.textContent='Das Teilen-Menü konnte nicht geöffnet werden.'}};
    setTimeout(()=>email.focus(),250)
  }
  async function renderPeople(c){
    css();
    c.innerHTML=`<div class="pc-head"><div><h2>Teilnehmer</h2><p>Eure Reisegruppe, Live-Status und gemeinsame Erinnerungen.</p></div><button class="pc-btn primary" data-people-invite>＋ Einladen</button></div><div class="people-now" data-people-now><div class="people-now-dot idle"></div><div><small>JETZT GERADE</small><b>Live-Status wird geladen …</b></div></div><div class="people-grid" data-people-list><div class="pc-card">Teilnehmer werden geladen …</div></div><section class="people-feed-section"><h2 class="people-feed-title">Reiseaktivitäten</h2><p class="people-feed-subtitle">Nur besondere Aktionen und gemeinsame Veränderungen – ohne technische Klicks oder Synchronisationsmeldungen.</p><div class="people-filters" data-feed-filters>${[['all','Alle','✦'],['gallery','Fotos','▣'],['moment','Momente','♥'],['budget','Budget','€'],['planning','Planung','✓'],['people','Teilnehmer','◉'],['location','Orte','⌖']].map(([k,l,i])=>`<button class="people-filter ${k===currentFeedFilter?'active':''}" data-filter="${k}" aria-pressed="${k===currentFeedFilter}"><span class="people-filter-icon">${i}</span><span class="people-filter-label">${l}</span></button>`).join('')}</div><div class="people-feed" data-people-feed>Aktivitäten werden geladen …</div></section>`;
    c.querySelector('[data-people-invite]').onclick=openInviteDialog;
    setActivity('people','ist im Teilnehmerbereich');
    try{
      const [list,events]=await Promise.all([participants(),feed()]);const mine=cloud()?.userId;
      updateNowBanner(c.querySelector('[data-people-now]'),list);
      c.querySelector('[data-people-list]').innerHTML=participantCards(list,mine);
      c.querySelectorAll('[data-show-map]').forEach(b=>b.onclick=()=>window.ParisProfileCenter?.open?.('map'));
      const feedNode=c.querySelector('[data-people-feed]');renderEventRows(feedNode,events,currentFeedFilter);
      c.querySelector('[data-feed-filters]').onclick=e=>{const f=e.target.dataset.filter;if(!f)return;currentFeedFilter=f;c.querySelectorAll('.people-filter').forEach(x=>{const on=x.dataset.filter===f;x.classList.toggle('active',on);x.setAttribute('aria-pressed',String(on))});renderEventRows(feedNode,events,f)};
      subscribeRealtime(()=>{if(c?.querySelector('[data-people-list]'))refreshPeopleView(c)});
    }catch(e){c.querySelector('[data-people-list]').innerHTML=`<div class="pc-note">Teilnehmerdaten konnten nicht geladen werden: ${esc(e.message)}<br>Bitte zuerst die neue SQL-Datei in Supabase ausführen.</div>`}
  }
  async function renderProfile(c,id){css();const p=profile(),fake={member_name:id?.memberName||'?',avatar_data:p.avatarData,avatar_color:p.avatarColor};c.innerHTML=`<div class="pc-head"><div><h2>Mein Profil</h2><p>Avatar, Name, Gerät und Freigaben dieses Reiseprofils.</p></div></div><div class="pc-card people-profile-editor">${avatarHtml(fake,true)}<div><div class="people-name">${esc(id?.memberName||'Nicht eingerichtet')}</div><div class="people-role">${id?.role==='owner'?'👑 Reisebesitzer':'❤️ Mitreisend'} · ${device().icon} ${device().type}</div><div class="pc-actions" style="justify-content:flex-start;margin-top:13px"><label class="pc-btn primary">Bild auswählen<input type="file" accept="image/*" data-avatar-file hidden></label><button class="pc-btn" data-avatar-initials>Initialen verwenden</button></div></div></div><div class="pc-card" style="margin-top:14px"><div class="pc-row"><div><b>Avatarfarbe</b><div class="pc-meta">Wird für Initialen und Kartenmarker verwendet.</div></div><div class="people-colors" data-colors></div></div><div class="pc-row"><div><b>Live-Standort teilen</b><div class="pc-meta">Nur Teilnehmer dieser Reise sehen deinen letzten freigegebenen Standort.</div></div><div class="pc-switch ${p.locationSharing?'on':''}" data-share></div></div><div class="pc-row"><b>Gerät</b><span>${device().icon} ${device().type} · ${esc(device().platform)}</span></div></div>`;const colors=['#e76f91','#bc6c9c','#6b8fd6','#4ca7a0','#df9b48','#7c8f5c','#596b7b','#d56b61'];const box=c.querySelector('[data-colors]');box.innerHTML=colors.map(x=>`<button class="people-color ${x===p.avatarColor?'active':''}" style="background:${x}" data-color="${x}" aria-label="Farbe"></button>`).join('');box.onclick=async e=>{const x=e.target.dataset.color;if(!x)return;const q=profile();q.avatarColor=x;saveProfile(q);await ensureProfile();renderProfile(c,id)};c.querySelector('[data-avatar-initials]').onclick=async()=>{const q=profile();q.avatarData=null;saveProfile(q);localStorage.setItem('parisPeopleClearAvatarV1','1');try{await rpc('paris_upsert_member_profile',{p_trip_id:id.tripId,p_member_name:id.memberName,p_avatar_data:'',p_avatar_color:q.avatarColor,p_location_sharing:q.locationSharing})}catch{}renderProfile(c,id)};c.querySelector('[data-avatar-file]').onchange=async e=>{const f=e.target.files?.[0];if(!f)return;const data=await resizeImage(f);const q=profile();q.avatarData=data;saveProfile(q);await ensureProfile();setActivity('profile','hat das Profilbild geändert',{feed:true,type:'profile.avatar'});renderProfile(c,id)};c.querySelector('[data-share]').onclick=async e=>{e.currentTarget.classList.toggle('on');const on=e.currentTarget.classList.contains('on');try{await setLocationSharing(on)}catch(err){alert(err.message);e.currentTarget.classList.toggle('on',!on)}};setActivity('profile','bearbeitet das eigene Profil')}
  function resizeImage(file){return new Promise((resolve,reject)=>{const img=new Image(),url=URL.createObjectURL(file);img.onload=()=>{const c=document.createElement('canvas'),size=256;c.width=c.height=size;const ctx=c.getContext('2d'),r=Math.max(size/img.width,size/img.height),w=img.width*r,h=img.height*r;ctx.drawImage(img,(size-w)/2,(size-h)/2,w,h);URL.revokeObjectURL(url);resolve(c.toDataURL('image/jpeg',.82))};img.onerror=reject;img.src=url})}
  async function loadLeaflet(){if(window.L)return;await new Promise((res,rej)=>{const l=document.createElement('link');l.rel='stylesheet';l.href='https://unpkg.com/leaflet@1.9.4/dist/leaflet.css';document.head.appendChild(l);const s=document.createElement('script');s.src='https://unpkg.com/leaflet@1.9.4/dist/leaflet.js';s.onload=res;s.onerror=rej;document.head.appendChild(s)})}
  async function renderMap(c){css();const p=profile();c.innerHTML=`<div class="pc-head"><div><h2>Live-Karte</h2><p>Die zuletzt freigegebenen Positionen eurer Reiseteilnehmer.</p></div></div><div class="people-map-toolbar"><button class="pc-btn ${p.locationSharing?'danger':'primary'}" data-map-share>${p.locationSharing?'Standortfreigabe beenden':'📍 Meinen Standort teilen'}</button><button class="pc-btn" data-map-refresh>Aktualisieren</button><span class="pc-badge">Leaflet · OpenStreetMap</span></div><div class="people-map" data-map><div class="people-map-empty">Karte wird geladen …</div></div><div class="pc-note" style="margin-top:12px">Die Karte zeigt nur ausdrücklich freigegebene Standorte. OpenStreetMap-Kartenmaterial wird mit sichtbarer Quellenangabe geladen.</div>`;setActivity('map','schaut auf die Live-Karte');c.querySelector('[data-map-share]').onclick=async e=>{const on=!profile().locationSharing;e.currentTarget.disabled=true;try{await setLocationSharing(on);renderMap(c)}catch(err){alert(err.message);e.currentTarget.disabled=false}};c.querySelector('[data-map-refresh]').onclick=()=>renderMap(c);try{const list=await participants(),located=list.filter(x=>Number.isFinite(x.latitude)&&Number.isFinite(x.longitude));await loadLeaflet();const node=c.querySelector('[data-map]');node.innerHTML='';const map=L.map(node,{zoomControl:true});L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'&copy; OpenStreetMap contributors'}).addTo(map);if(!located.length){map.setView([48.8566,2.3522],11);L.popup().setLatLng([48.8566,2.3522]).setContent('Noch kein Standort wurde freigegeben.').openOn(map)}else{const bounds=[];located.forEach(m=>{const initials=(m.member_name||'?').split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase();const content=m.avatar_data?`<img src="${esc(m.avatar_data)}" alt="">`:esc(initials);const icon=L.divIcon({className:'',html:`<div class="people-map-marker" style="--avatar:${esc(m.avatar_color||'#e76f91')}">${content}</div>`,iconSize:[46,46],iconAnchor:[23,23]});L.marker([m.latitude,m.longitude],{icon}).addTo(map).bindPopup(`<div class="people-popup-name">${esc(m.member_name)}</div><div class="people-popup-meta">📍 ${esc(m.place_label||'Freigegebener Standort')}<br>${ago(m.location_updated_at)} · ± ${Math.round(m.accuracy||0)} m</div>`);bounds.push([m.latitude,m.longitude])});if(bounds.length===1)map.setView(bounds[0],15);else map.fitBounds(bounds,{padding:[45,45]});if(located.length>1){const d=distance(located[0],located[1]);if(d){const dc=L.control({position:'bottomleft'});dc.onAdd=()=>{const x=L.DomUtil.create('div','pc-badge');x.style.background='#fff';x.innerHTML=`Abstand: ${d<1000?Math.round(d)+' m':(d/1000).toFixed(1)+' km'}`;return x};dc.addTo(map)}}}setTimeout(()=>map.invalidateSize(),100)}catch(e){c.querySelector('[data-map]').innerHTML=`<div class="people-map-empty">Die Live-Karte konnte nicht geladen werden.<br>${esc(e.message)}<br><br>Bitte die SQL-Einrichtung prüfen.</div>`}}
  function describeTarget(){return null}
  function inferActivity(){}
  function trackFormChange(){}
  function subscribeRealtime(onChange){const id=identity(),client=cloud()?.client;if(!id?.tripId||!client?.channel)return;if(realtimeChannel)client.removeChannel?.(realtimeChannel);let timer;realtimeChannel=client.channel(`paris-events-${id.tripId}`).on('postgres_changes',{event:'*',schema:'public',table:'paris_member_activity_feed',filter:`trip_id=eq.${id.tripId}`},payload=>{window.dispatchEvent(new CustomEvent('paris:event-realtime',{detail:payload}));clearTimeout(timer);timer=setTimeout(onChange,120)}).on('postgres_changes',{event:'*',schema:'public',table:'paris_member_presence',filter:`trip_id=eq.${id.tripId}`},()=>{clearTimeout(timer);timer=setTimeout(onChange,180)}).subscribe()}
  async function init(){try{await cloud()?.ready;if(!identity()?.tripId)return;await ensureProfile();lastActivity={key:'general',text:'ist in der Paris-App',at:Date.now()};await heartbeat();heartbeatTimer=setInterval(()=>heartbeat(),15000);locationTimer=setInterval(pushLocation,20000);window.addEventListener('paris:location-state',pushLocation);window.addEventListener('paris-location-updated',pushLocation);document.addEventListener('visibilitychange',()=>{if(!document.hidden)heartbeat()});window.addEventListener('online',()=>heartbeat());let galleryCount=null;document.addEventListener('paris:gallery-updated',e=>{const n=Number(e.detail?.count||0);if(galleryCount!==null&&n>galleryCount)track('gallery.upload',{category:'gallery',text:`hat ${n-galleryCount===1?'ein neues Foto':(n-galleryCount)+' neue Fotos'} hinzugefügt`,aggregateKey:'gallery-upload',aggregateWindowSeconds:18,metadata:{added:n-galleryCount,total:n}});else if(galleryCount!==null&&n<galleryCount)track('gallery.delete',{category:'gallery',text:`hat ${galleryCount-n===1?'ein Foto':(galleryCount-n)+' Fotos'} entfernt`,aggregateKey:'gallery-delete',aggregateWindowSeconds:12,metadata:{removed:galleryCount-n,total:n},live:false});galleryCount=n});document.addEventListener('paris:memory-added',e=>track('moment.create',{category:'moment',text:'hat einen neuen Moment erstellt',metadata:e.detail||{}}));document.addEventListener('paris:budget-added',e=>track('budget.add',{category:'budget',text:e.detail?.text||'hat eine Ausgabe hinzugefügt',metadata:e.detail||{}}));document.addEventListener('paris:budget-updated',e=>track('budget.change',{category:'budget',text:e.detail?.text||'hat das Budget aktualisiert',metadata:e.detail||{}}));document.addEventListener('paris:planning-updated',e=>track('planning.change',{category:'planning',text:e.detail?.text||'hat die gemeinsame Planung geändert',metadata:e.detail||{}}));document.addEventListener('paris:day-closed',e=>track('closure.complete',{category:'closure',text:'hat den Tagesabschluss geschrieben',metadata:e.detail||{}}))}catch(e){console.warn('People-System:',e.message)}}
  window.ParisEvents={track,setCurrent:(category,text)=>setActivity(category,text),subscribeRealtime};
  window.ParisPeople={renderPeople,renderProfile,renderMap,setActivity,log:(k,t,opts={})=>track(opts.type||`${k}.action`,{category:k,text:t,...opts}),participants,setLocationSharing,getProfile:profile};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
